# EKYVA application
NODE_ENV=development
SESSION_SECRET=replace-with-a-long-random-value
EKYVA_ADMIN_USER_IDS=

# Provider credentials are optional in local development. Add one or both
# to enable live generation and automatic failover.
OPENAI_API_KEY=
EKYVA_OPENAI_BASE_URL=https://api.openai.com
ANTHROPIC_API_KEY=

# Managed by Replit when the database is provisioned.
DATABASE_URL=